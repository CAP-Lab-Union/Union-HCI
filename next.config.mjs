/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/Union-HCI',
    assetPrefix: '/Union-HCI/',
    output: 'export',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;